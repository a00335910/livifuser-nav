{
  "sweep_id": "pilot5_leave_one_episode_out_v1",
  "preregistration": "docs/experiments/PREREGISTRATION_PILOT30.md (freezing commit recorded in docs/PROJECT_STATE.md)",
  "seed_note": "Three fixed seeds per model per leave-one-episode-out fold; every result is reported.",
  "seeds": [20260805, 20260806, 20260807],
  "runs": [
    {"name": "full", "variant": "full", "loss": "heteroscedastic"},
    {"name": "lidar_only", "variant": "lidar_only", "loss": "heteroscedastic"},
    {"name": "rgb_only", "variant": "rgb_only", "loss": "heteroscedastic"},
    {"name": "concat", "variant": "concat", "loss": "heteroscedastic"},
    {"name": "no_fov_mask", "variant": "no_fov_mask", "loss": "heteroscedastic"},
    {"name": "no_gate", "variant": "no_gate", "loss": "heteroscedastic"},
    {"name": "no_temporal", "variant": "no_temporal", "loss": "heteroscedastic"},
    {"name": "full_mean_only", "variant": "full", "loss": "mean_only"}
  ],
  "context_k": 8,
  "horizon_h": 8,
  "lidar_sectors": 80,
  "lidar_range_clip_m": 10.0,
  "visual_mask_radius_tokens": 1,
  "batch_size": 16,
  "warmup_steps": 3000,
  "nll_steps": 1000,
  "warmup_learning_rate": 0.001,
  "nll_learning_rate": 0.00025,
  "lr_schedule": "cosine_per_phase",
  "lr_floor_fraction": 0.05,
  "weight_decay": 0.0,
  "gradient_clip_norm": 1.0,
  "torch_threads": 6,
  "eval_batch_size": 64,
  "log_every_steps": 100,
  "mahalanobis_shrinkage": 0.1,
  "loss_note": "mean_only trains warmup_steps + nll_steps entirely on the mean loss so every run receives the same optimization budget; its NLL metrics are reported but reflect an untrained variance head.",
  "temporary_backbone": {
    "architecture": "DINOv3 ViT-S/16",
    "frozen": true,
    "locked_final_deviation": "Temporary pipeline baseline only; Architecture v1.1 requires frozen DINOv3 ViT-S+/16. Never relabel this artifact as S+/16."
  }
}
